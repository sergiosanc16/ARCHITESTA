{% extends 'base.html.twig' %}

{% block title %}TestaToficio{% endblock %}

{% block body %}
    <h1>TestaToficio</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ testa_toficio.id }}</td>
            </tr>
            <tr>
                <th>Des_oficio</th>
                <td>{{ testa_toficio.desOficio }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_testa_toficio_index') }}">back to list</a>

    <a href="{{ path('app_testa_toficio_edit', {'id': testa_toficio.id}) }}">edit</a>

    {{ include('testa_toficio/_delete_form.html.twig') }}
{% endblock %}
