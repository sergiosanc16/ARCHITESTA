{% extends 'base.html.twig' %}

{% block title %}TestaTparentesco{% endblock %}

{% block body %}
    <h1>TestaTparentesco</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ testa_tparentesco.id }}</td>
            </tr>
            <tr>
                <th>Des_parentesco</th>
                <td>{{ testa_tparentesco.desParentesco }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_testa_tparentesco_index') }}">back to list</a>

    <a href="{{ path('app_testa_tparentesco_edit', {'id': testa_tparentesco.id}) }}">edit</a>

    {{ include('testa_tparentesco/_delete_form.html.twig') }}
{% endblock %}
