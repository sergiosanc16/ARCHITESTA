<form method="post" action="{{ path('app_testa_ttestamento_delete', {'id': testa_ttestamento.id}) }}" onsubmit="return confirm('¿Estás segur@ de borrar este registro?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ testa_ttestamento.id) }}">
    <button type="submit" class="btn boton  mr-2 btn