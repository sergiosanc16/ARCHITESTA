{% extends 'base.html.twig' %}

{% block title %}TestaTValidacion{% endblock %}

{% block body %}
    <h1>TestaTValidacion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ testa_t_validacion.id }}</td>
            </tr>
            <tr>
                <th>Num_validacion</th>
                <td>{{ testa_t_validacion.numValidacion }}</td>
            </tr>
            <tr>
                <th>Validaciones</th>
                <td>{{ testa_t_validacion.validaciones ? testa_t_validacion.validaciones|join(', ') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_testa_t_validacion_index') }}">back to list</a>

    <a href="{{ path('app_testa_t_validacion_edit', {'id': testa_t_validacion.id}) }}">edit</a>

    {{ include('testa_t_validacion/_delete_form.html.twig') }}
{% endblock %}
