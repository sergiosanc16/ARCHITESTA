<form method="post" action="{{ path('app_testa_traw_delete', {'id': testa_traw.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ testa_traw.id) }}">
    <button class="btn">Delete</button>
</form>
